{
  "builds": [
    {
      "src": "signal-slot/package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "build", "buildCommand": "npm ci && npm run build" }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}